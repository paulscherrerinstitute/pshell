package ch.psi.pshell.swing;

import ch.psi.pshell.sequencer.Sequencer;
import ch.psi.pshell.utils.State;
import java.awt.Component;
import java.util.HashMap;
import javax.swing.JButton;
import javax.swing.JSpinner;
import javax.swing.text.JTextComponent;
import ch.psi.pshell.sequencer.SequencerListener;

/**
 *
 */
public class ScriptButton extends MonitoredPanel {


    public ScriptButton() {
        initComponents();
    }

    SequencerListener sequencerListener;

    @Override
    protected void onShow() {
        sequencerListener = new SequencerListener() {
            @Override
            public void onStateChanged(State state, State former) {
                button.setEnabled(state.isReady());
            }
        };
        Sequencer.getInstance().addListener(sequencerListener);
        button.setEnabled(Sequencer.getInstance().getState().isReady());
    }

    @Override
    protected void onHide() {
        Sequencer.getInstance().addListener(sequencerListener);
    }

    /**
     * This method is called from within the constructor to initialize the form. WARNING: Do NOT
     * modify this code. The content of this method is always regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        button = new javax.swing.JButton();

        button.setText("Start");
        button.addActionListener(new java.awt.event.ActionListener() {
            public void actionPerformed(java.awt.event.ActionEvent evt) {
                buttonActionPerformed(evt);
            }
        });

        javax.swing.GroupLayout layout = new javax.swing.GroupLayout(this);
        this.setLayout(layout);
        layout.setHorizontalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addComponent(button, javax.swing.GroupLayout.DEFAULT_SIZE, 127, Short.MAX_VALUE)
        );
        layout.setVerticalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addComponent(button)
        );
    }// </editor-fold>//GEN-END:initComponents

    private void buttonActionPerformed(java.awt.event.ActionEvent evt) {//GEN-FIRST:event_buttonActionPerformed
        try {
            HashMap<String, Object> args = new HashMap<>();
            if (arguments != null) {
                for (String arg : arguments) {
                    Object val = null;
                    try {
                        Component c = SwingUtils.getComponentByName(getTopLevelAncestor(), arg);
                        if (c instanceof JSpinner jSpinner) {
                            val = jSpinner.getValue();
                        } else if (c instanceof JTextComponent jTextComponent) {
                            val = jTextComponent.getText();
                        } else if (c instanceof JButton jButton) {
                            val = jButton.isSelected();
                        }
                    } catch (Exception ex) {
                    }
                    args.put(arg, val);
                }
            }
            Sequencer.getInstance().evalFileAsync(script, args);
        } catch (Exception ex) {
            showException(ex);
        }
    }//GEN-LAST:event_buttonActionPerformed

    String script = "Script";

    public String getScript() {
        return script;
    }

    public void setScript(String script) {
        this.script = script;
    }

    public String getText() {
        return button.getText();
    }

    public void setText(String text) {
        button.setText(text);
    }

    String[] arguments;

    public String[] getArguments() {
        return arguments;
    }

    public void setArguments(String[] arguments) {
        this.arguments = arguments;
    }

    // Variables declaration - do not modify//GEN-BEGIN:variables
    private javax.swing.JButton button;
    // End of variables declaration//GEN-END:variables
}
